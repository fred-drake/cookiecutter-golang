{
    // Use IntelliSense to learn about possible attributes.
    // Hover to view descriptions of existing attributes.
    // For more information, visit: https://go.microsoft.com/fwlink/?linkid=830387
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch main.go",
            "type": "go",
            "request": "launch",
            "mode": "auto",
            "program": "cmd/{{ cookiecutter.project_name }}/main.go",
            // Place desired non-sensitive environment variables here for testing
            // that you want checked into git.
            "env": {
            },

            // Uncomment the line below if you need to set sensitive environment
            // variables for testing.  Create an .env file in the root project location
            // with variables set in KEY=VALUE form, one per line.  This .env file
            // will not be checked into version control.

            // "envFile": "/workspaces/{{ cookiecutter.project_name }}/.env"
        }
    ]
}
